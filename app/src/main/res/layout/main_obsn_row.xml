<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="onvm"
            type="me.samen.d2.view.obsnote.ONoteVM" />

        <variable
            name="obsnote"
            type="me.samen.d2.data.entities.ObsNote" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:clickable="true"
        bind:tparent="@{obsnote}"
        bind:vm="@{onvm}">


        <TextView
            android:id="@+id/tdesc"
            style="@style/tdesc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:maxLines="3"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            bind:tdesc="@{obsnote}"
            bind:vm="@{onvm}" />


        <TextView
            android:id="@+id/tbullets"
            style="@style/tbullets"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tdesc"
            bind:tbullets="@{obsnote}"
            bind:vm="@{onvm}" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>